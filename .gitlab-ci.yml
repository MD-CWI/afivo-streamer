image: gcc

before_script:
  - apt-get update -qq && apt-get install -y -qq python3-numpy
  - gfortran --version

cache:
  paths:
    - afivo

build:
  script:
    - make -j

run_tests:
  script:
    - ./run_test.sh
